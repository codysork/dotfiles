#!/bin/bash

source "${HOME}/.cache/wal/colors.sh"
curHour="$(date +'%H')"
sunrise=6
sunset=20


styli.sh -p -w 1920 -h 1080 -s "christmas"
sh ~/.local/bin/dunstwal
notify-send "Changing color scheme..."
wal_steam -w
sh ~/Scripts/wal-discord/wal-discord
sh ~/.local/bin/keycolors
